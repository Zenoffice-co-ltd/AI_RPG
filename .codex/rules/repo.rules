prefix_rule(
    pattern = ["git", "reset", "--hard"],
    decision = "forbidden",
    justification = "Do not discard worktree state with `git reset --hard`. Use a targeted revert or reviewed recovery plan instead.",
    match = [
        "git reset --hard",
        "git reset --hard HEAD~1",
    ],
    not_match = [
        "git reset --soft HEAD~1",
    ],
)

prefix_rule(
    pattern = ["git", "checkout", "--"],
    decision = "forbidden",
    justification = "Do not use `git checkout --` to discard local changes. Restore specific files with a reviewed action instead.",
    match = [
        "git checkout -- README.md",
        "git checkout -- packages/scenario-engine/src/compileAccountingScenario.ts",
    ],
    not_match = [
        "git checkout codex/accounting-phase34-v2",
    ],
)

prefix_rule(
    pattern = ["git", "clean", "-fd"],
    decision = "forbidden",
    justification = "Do not remove untracked files with `git clean -fd`. Generated evidence and local artifacts must be reviewed before cleanup.",
    match = [
        "git clean -fd",
        "git clean -fd tmp",
    ],
    not_match = [
        "git clean -n",
    ],
)

prefix_rule(
    pattern = ["pnpm", "publish:scenario"],
    decision = "prompt",
    justification = "Publishing mutates external vendor state. Confirm the scenario id, environment, and readiness evidence before running it.",
    match = [
        "pnpm publish:scenario -- --scenario staffing_order_hearing_busy_manager_medium",
        "pnpm publish:scenario -- --scenario accounting_clerk_enterprise_ap_busy_manager_medium",
    ],
    not_match = [
        "pnpm compile:scenarios",
    ],
)

prefix_rule(
    pattern = ["git", "merge"],
    decision = "prompt",
    justification = "Before merging, update README/docs for workflow or behavior changes, refresh repo skills or Codex safety config if the workflow changed, and confirm current verification evidence.",
    match = [
        "git merge codex/accounting-phase34-v2",
        "git merge --no-ff main",
    ],
    not_match = [
        "git merge-base main HEAD",
    ],
)

prefix_rule(
    pattern = ["gh", "pr", "merge"],
    decision = "prompt",
    justification = "Before merging a PR, confirm docs/runbooks, repo skills, Codex safety config, and release verification evidence are updated for the final state.",
    match = [
        "gh pr merge 5 --squash",
        "gh pr merge https://github.com/iwase-cpu/AI_RPG/pull/5 --merge",
    ],
    not_match = [
        "gh pr view 5",
    ],
)
