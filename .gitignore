node_modules
.next
.turbo
coverage
dist
playwright-report
test-results
.env
.env.local
.env.*.local
!.env.local.example
firebase-debug.log
*.tsbuildinfo
*.log
data/generated/voice-benchmark/
data/generated/tts-provider-benchmark/
data/generated/tts-response-latency-benchmark/
data/generated/llm-model-latency/
data/generated/quality-latency-benchmark/
data/generated/chat-orb-sessions/
# Registered speech intermediate outputs (build pipeline staging area).
# Only the promoted `v1/` manifest + artifacts are tracked.
# `source.json` is the input spec and IS tracked.
data/generated/registered-speech/v1.candidate/artifacts/
data/generated/registered-speech/v1.candidate/manifest.json
# Build pipeline reports + wav previews are transient (one folder per
# build attempt). Reviewers download them from `out/` locally; they are
# never committed.
out/registered-speech-build/
out/grok_first_v50_browser_dod_e2e/
out/grok_first_v50_live_e2e/
# Ignore Claude Code local state (worktrees, scheduled task locks, etc.).
# Carve out .claude/rules/ so the cross-tool rules surface (mirrors
# AGENTS.md SoT) is committed alongside .codex/rules/ and .cursor/rules/.
.claude/*
!.claude/rules/
!.claude/rules/**
